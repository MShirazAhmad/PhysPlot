# Fit Functions

Legacy curve-fit models. Each file defines `DISPLAY_NAME`, `DEFAULT_LABEL`,
and `KIND` (`"poly"` with `DEGREE`, or `"callable"` with `function(x, ...)`).
Simple Mode's LSQ fit accepts a free-form expression instead and does not need
these files.
