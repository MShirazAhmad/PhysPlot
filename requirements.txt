# PhysPlot runtime requirements.
#
# For source development, prefer:
#
#   python -m pip install -e ".[dev]"
#
# This file is kept for tools and environments that install from
# requirements.txt directly. It mirrors the runtime dependencies in
# pyproject.toml.

FigureForge>=0.3.3
matplotlib>=3.8
numpy>=1.24
openpyxl>=3.1
pandas>=2.0
PyQt6>=6.6
scipy>=1.10
